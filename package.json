{
  "name": "kuroshiro-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker converting Japanese text via Kuroshiro (Hiragana/Katakana/Romaji), simple REST + OpenAI‑compatible streaming",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=es2020 --format=esm --outfile=dist/index.js",
    "dev":  "npm run build && wrangler dev",
    "deploy":"npm run build && wrangler deploy"
  },
  "dependencies": {
    "kuroshiro": "^1.2.0",
    "kuroshiro-analyzer-kuromoji": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
